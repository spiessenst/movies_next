import styles_global from "../styles/globals.scss";
import styles from "../styles/style.scss";

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
